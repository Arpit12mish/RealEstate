package com.brandPitara.sfs.analytics.domain;

/** Client platform an event originated from - shared taxonomy across mobile and the future web client. */
public enum AnalyticsSourcePlatform {
    ANDROID,
    IOS,
    WEB
}
